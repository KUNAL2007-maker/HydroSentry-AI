# HydroSentry-AI — runtime dependencies
# Demo mode runs fully offline; Live mode adds a keyless real-time fetch.
streamlit>=1.36
plotly>=5.22
numpy>=1.24
pandas>=2.0             # app.py — map marker frames (pydeck)
pydeck>=0.9             # app.py — interactive basin & evacuation maps (tokenless CARTO)
requests>=2.31          # live_data.py — real-time Open-Meteo fetch + geocoding (Live mode)
